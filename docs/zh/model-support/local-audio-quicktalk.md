# 本地语音 + QuickTalk

完整教程已经移到[模型部署 / 本地语音 + QuickTalk](../model-deployment/recipes/local-quicktalk-audio.md)。

本页保留是为了兼容旧链接。新的文档结构中：

- `模型支持` 用于解释模型能力、后端选择和适用场景。
- `模型部署` 用于放置权重、依赖、环境变量、启动命令和验证步骤。

如果你要部署本地 SenseVoiceSmall、本地 CosyVoice3 和 QuickTalk local 链路，请直接阅读[模型部署 / 本地语音 + QuickTalk](../model-deployment/recipes/local-quicktalk-audio.md)。
