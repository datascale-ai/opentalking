# API Reference

- `GET /health`
- `GET /models`
- `GET /avatars`
- `POST /sessions`
- `POST /sessions/{id}/speak`
- `POST /sessions/{id}/chat` — QuickTalk sessions only; streams LLM output through TTS and realtime rendering.
- `POST /sessions/{id}/interrupt`
- `POST /sessions/{id}/webrtc/offer`
- `GET /sessions/{id}/events`
