# Avatar Assets

This GitHub-ready snapshot keeps the avatar assets needed by the current runtime.

Included per avatar when available:

- `manifest.json`
- `preview.png`
- `reference.png`
- `frames/`
- `full_frames/`
- `prepared/`
- `.flashtalk_idle_cache*.npz`

The missing part for actual local execution is the model weights under the repository-level `models/` directory.
